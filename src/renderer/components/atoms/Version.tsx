import styled from 'styled-components';

export const VersionName = styled.span`
`;

export const VersionNumber = styled.span`
  font-weight: 400;
`;

export const Version = styled.li`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
`;