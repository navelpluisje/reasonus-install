// import { Listener } from '../../types';
// import { setFocus } from '../store/window';
// import { setIni } from '../store/ini';
// import { useAppDispatch } from '../store/hooks';
// import { useEffect } from 'react';

export const useCsiApi = () => {
  // const dispatch = useAppDispatch();

  // const setAppFocus: Listener<boolean> = (_, focus) => dispatch(setFocus(focus));

  // const getIni = async () => {
  //   const result = await window.electronAPI.getCsiIni();
  //   dispatch(setIni(result));
  // };

  // useEffect(() => {
  //   window.electronAPI.appFocus(setAppFocus);
  //   getIni();
  // });

};
